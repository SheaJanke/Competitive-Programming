#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    scanf("%d", &t);
    for(int k = 0; k < t; k++){
        int x, y;
        scanf("%d %d", &x, &y);
        printf("%d %d\n", x-1, y);
    }
}